@model PRN232_WebClient_Tachonogy.DTOs.ResetPasswordRequest
@{
    ViewData["Title"] = "Reset Password";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm border-0 rounded-3">
                <div class="card-body p-4">
                    <h3 class="text-center mb-4">Reset Your Password</h3>

                    <form asp-action="ResetPassword" method="post" novalidate>
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Token" />

                        <div class="form-group mb-3">
                            <label asp-for="NewPassword" class="form-label"></label>
                            <input asp-for="NewPassword" class="form-control" type="password" placeholder="Enter new password" />
                            <span asp-validation-for="NewPassword" class="text-danger small"></span>
                        </div>

                        <button type="submit" class="btn btn-success w-100">Reset Password</button>

                        @if (ViewBag.ErrorMessages != null)
                        {
                            <div class="alert alert-danger mt-3 text-center">@ViewBag.ErrorMessages</div>
                        }

                        @if (TempData["SuccessMessages"] != null)
                        {
                            <div class="alert alert-success mt-3 text-center">@TempData["SuccessMessages"]</div>
                        }
                    </form>

                    <div class="text-center mt-3">
                        <a asp-action="Login" class="text-decoration-none">← Back to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}